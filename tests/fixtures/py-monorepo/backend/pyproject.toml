[project]
name = "ledger-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi", "pydantic"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
