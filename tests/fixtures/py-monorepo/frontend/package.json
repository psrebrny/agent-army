{
  "name": "ledger-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "lint": "eslint src",
    "build": "vite build"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "eslint": "^8.0.0",
    "vite": "^5.0.0"
  }
}
