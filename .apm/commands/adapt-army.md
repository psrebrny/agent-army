---
description: Propagate a new durable guideline or architectural correction into the whole agent team consistently (routes to every agent that owns it, updates AGENTS.md). Not for one-off task tweaks.
---
Follow the instructions in `.agents/skills/adapt-army/SKILL.md`. If that file is missing, report an incomplete Agent Army installation and ask the user to run `apm install` or `apm update`; never guess a tool-specific skill directory.
