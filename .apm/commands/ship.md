---
description: Full delivery pipeline for a task: discovery → blueprint → TDD → review. Pass the task description as argument.
---
Follow the instructions in `.agents/skills/ship/SKILL.md`. If that file is missing, report an incomplete Agent Army installation and ask the user to run `apm install` or `apm update`; never guess a tool-specific skill directory.
